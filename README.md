# Akhil Resume Website

## Run locally
```bash
npm install
npm run dev
```

## Build for deployment
```bash
npm run build
```

The production files are generated in `dist/`.

## Netlify
Import this project into Netlify and use:
- Build command: `npm run build`
- Publish directory: `dist`
